/** Type declarations for the shared browser probe (probe.js). The probe
 * source is a single template-literal export consumed by Deno, Rust
 * (include_str!) and the browser; see the header comment in probe.js. */
export const PROBE_SOURCE: string;
