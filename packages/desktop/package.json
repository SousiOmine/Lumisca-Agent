{
  "name": "@lumisca/desktop",
  "private": true,
  "version": "0.5.4",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build:server": "deno run --allow-all ../../scripts/build-desktop-assets.ts && node -e \"['lumisca-server.exe','lumisca-server'].forEach(n => require('node:fs').rmSync('src-tauri/resources/server/' + n, { force: true }))\" && deno compile --allow-net --allow-read --allow-write --allow-env --allow-run --allow-sys --output src-tauri/resources/server/lumisca-server ../../packages/server/mod.ts",
    "build:server:dev": "deno run --allow-all ../../scripts/build-desktop-assets.ts"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
