{
  "name": "@lumisca/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build:server": "deno run --allow-all ../../scripts/build-desktop-assets.ts && deno compile --allow-net --allow-read --allow-write --allow-env --allow-run --allow-sys --output src-tauri/resources/server/lumisca-server.exe ../../packages/server/mod.ts",
    "build:server:dev": "deno run --allow-all ../../scripts/build-desktop-assets.ts"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
