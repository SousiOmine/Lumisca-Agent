[package]
name = "lumisca-browser-rpc"
version = "0.1.0"
description = "Shared browser-lab RPC protocol pieces for the Lumisca desktop shell and the CLI browser host"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"