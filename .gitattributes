# Normalize line endings to LF: the repo is deno-formatted (LF) and deno fmt
# must produce a clean diff on every platform (git autocrlf would otherwise
# break fmt:check on Windows checkouts).
* text=auto eol=lf
